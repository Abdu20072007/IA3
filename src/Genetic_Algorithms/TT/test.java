package Genetic_Algorithms.TT;

import java.util.Objects;
import Genetic_Algorithms.TT.TTData;

public class test {

    public static void main(String[] args) {

        // -------------------------------------------------------------
        // int[] randInts = new int[10];

        // randInts = Utilities.randDistinctInts(10, 100);

        // for (int i = 0; i < 10; i++) {
        // System.out.println("Random Distinct Integers: " + randInts[i]);
        // }

        // // --------------------------------------------------------------------
        // if (Objects.equals("a", "a")) {
        // System.out.println(".");
        // }

        // ------------------------------------------------------------------

        // Student[] students = new Student[3];
        // students[2] = new Student("John", TTData.subjects, 0, 1, 2, 3, 4, 5, 6, 11);

        // int i = 0;
        // while (i < 8) {
        // System.out.println(TTData.students[1].stSubj[i]);
        // i++;
        // }

        // ---------------------------------------------------------------------------------

        // int lessonsPerPeriod = 0;
        // int fitnessScore = 0;
        // String[] slot = new String[6];
        // slot[0] = "Free";
        // slot[1] = "Y9Physics";
        // slot[2] = "Y9Religion";
        // slot[3] = "Free";
        // slot[4] = "Free";
        // slot[5] = "Free";

        // for (int k = 0; k < Timetables.lessons; k++) {
        // for (int j2 = 0; j2 < 8; j2++) {

        // if (Objects.equals(TTData.students[2].stSubj[j2], slot[k])) {

        // lessonsPerPeriod++;

        // }
        // }

        // }
        // if (lessonsPerPeriod != 1)
        // fitnessScore = fitnessScore + Math.abs((lessonsPerPeriod - 1));

        // if (fitnessScore == 1) {
        // System.out.print(".");
        // }

        // ------------------------------------------------------------------------

    }
}